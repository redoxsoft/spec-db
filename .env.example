# Optional — standalone Import deep-link bases (kind + slug only; no contentUrl / workspace).
# SpecX: templates + specs. WorkX: pipelines. Local: http://localhost:<port>/import
# VITE_SPECX_IMPORT_BASE=https://specx.redoxsoft.com/import
# VITE_WORKX_IMPORT_BASE=https://workx.redoxsoft.com/import

# Optional — View Source GitHub tree base (no trailing slash).
# VITE_GITHUB_TREE_BASE=https://github.com/redoxsoft/spec-db/tree/main
