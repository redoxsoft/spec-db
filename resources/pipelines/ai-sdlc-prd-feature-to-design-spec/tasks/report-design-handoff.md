**Goal:** Report the Design Spec handoff: document title, version, unresolved decisions, and milestone planning as the next step.

**Scope:** Read-only reporting of the locked `<Subject> - Design Spec`. Do not create or edit documents or code. Do not commit, push, open a pull request, publish, or deploy.

**Verification / Definition of Done:** The output names the Design Spec title and version, the locked `${SOURCE_SPEC_REF}`, remaining open decisions, and recommends milestone planning. No new files or documents were created.

**Instructions:**

- Reread the document titled `<Subject> - Design Spec` so the report reflects the current document, not a previous task's memory. If that title is unavailable, repeat resolution rather than guessing.
- Report the exact title and version, the locked `${SOURCE_SPEC_REF}`, any unresolved decisions, and the recommended next step: generate a Milestone Plan from this Design Spec by running `Milestone Plan Builder`.
- Do not start another pipeline unless the user asks.
