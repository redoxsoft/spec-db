**Goal:** Establish the real starting point for planning.

**Scope:** Read SpecX specs (requirements, `${PROJECT_NAME} - Decision Matrix`, `${PROJECT_NAME} - Architecture Rules`, SpecX spec Planning Rules, SpecX spec Pipeline Operating Contract) and scaffold documentation/source. Do not implement features or rewrite the scaffold. Do not write local markdown plans.

**Definition of Done:** Completed scaffold work, missing work, deferred work, and relevant constraints are recorded, with no unresolved SpecX input ambiguity. SpecX spec `${PROJECT_NAME} - Architecture Rules` is present (hard-stop if missing).

Actions:

- Resolve and read all required SpecX specs for `${PROJECT_NAME}`, including the mandatory architecture-rules SpecX spec.
- Inspect the scaffold, if present.
- Identify what is already complete, what is missing, and what is explicitly deferred.
- Do not plan scaffolding work as a future feature when Scaffolding has already completed it.
