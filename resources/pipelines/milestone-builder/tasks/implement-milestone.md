**Goal:** Deliver the selected milestone's scoped behavior without scope creep.

**Scope:** Modify only files and dependencies needed for the selected milestone, while following SpecX spec `${PROJECT_NAME} - Decision Matrix` and SpecX spec `${PROJECT_NAME} - Architecture Rules`. Do not implement later milestones or unrelated refactors.

**Definition of Done:** All planned implementation work for the selected scope is present, the project remains structurally consistent, and the acceptance criteria are ready for verification.

Actions:

- Implement only the selected scope.
- Follow the locked stack and architecture rules from the SpecX specs.
- Keep the project runnable.
- Update required documentation, configuration, migrations, or seed data.
- Avoid speculative abstractions and unrelated refactors.
