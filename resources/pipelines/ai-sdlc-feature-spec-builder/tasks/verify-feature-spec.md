**Goal:** Confirm every change has acceptance criteria and every impacted section names its current contract and required update.

**Scope:** Read the locked `<Subject> - Change Request` and the SpecX spec `Work Intake & Artifact Selection Guide`. Record gaps. Do not start implementation. Do not commit, push, open a pull request, publish, or deploy.

**Verification / Definition of Done:** Each change has testable acceptance criteria. Each impacted section names its current contract and the required update. Delivery approach is stated. Gaps are listed, or the Change Request is reported ready.

**Instructions:**

- Resolve the SpecX spec `Work Intake & Artifact Selection Guide` by exact title. If it cannot be found, stop and report that exact missing title.
- Reread the document titled `<Subject> - Change Request` in full. If that title is unavailable, repeat resolution rather than guessing.
- Check the Change Request quality bar: current behaviour and desired outcome stated as deltas; every change has testable acceptance criteria; every impacted section names current contract and required update; delivery approach says whether the change can go straight to implementation.
- Escalate to a PRD and stop if new journeys, audiences, product-level metrics, or capability-creating architecture appeared during authoring.
- Report the document title and version, what passed, and any remaining gaps. Do not mark the spec ready while a quality-bar item fails.
