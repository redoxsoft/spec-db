**Goal:** Create or update the PRD for the locked initiative using the PRD Template, with assumptions and open questions labeled as such.

**Scope:** Create or update only the SpecX document titled `<Subject> - PRD`, using the locked `${INITIATIVE}` and the locked subject from resolution. Do not edit application code. Do not commit, push, open a pull request, publish, or deploy.

**Verification / Definition of Done:** A document titled `<Subject> - PRD` exists, was created or updated in place (no duplicate), and follows the `PRD Template`. Assumptions and open questions are labeled. The task stopped if the template could not be found by exact title.

**Instructions:**

- Resolve the SpecX template `PRD Template` by exact title. If it cannot be found, stop and report that exact missing title. Do not substitute another template or invent a structure.
- Reread the locked initiative and subject. If either locked value is unavailable, repeat resolution rather than guessing. Do not rely on a previous task's unstated memory.
- Locate an existing SpecX document titled `<Subject> - PRD` and update it. If none exists, create it with that exact title. Do not create a second document for the same subject.
- Fill the template from the locked `${INITIATIVE}` and any evidence gathered during resolution. State problem, audience, 1–3 measurable goals, and non-goals. Write journeys and requirements with testable acceptance criteria. Do not invent personas, metrics, or scope. Do not describe implementation.
- Label assumptions and open questions explicitly. Do not write guesses as facts.
- Report the document title and version, and any unresolved decisions that remain.
