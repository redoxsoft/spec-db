**Goal:** Prove that the blank project is operational and ready for Planning.

**Scope:** Run only the relevant install, type, lint, test, build, and smoke checks for the scaffold per SpecX spec Scaffolding Rules. Fix scaffold issues; do not implement product features to make checks pass.

**Definition of Done:** All applicable checks pass, or every failure is recorded with command output and the pipeline is marked incomplete.

Actions:

- Run the relevant install, type, lint, test, build, and smoke checks supported by the selected stack.
- Record exact commands and outcomes.
