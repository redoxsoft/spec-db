**Goal:** Obtain one validated answer for every applicable architecture decision.

**Scope:** Use only the decision nodes and recommendation rules in SpecX spec Architecture Decision Matrix. Ask multiple-choice questions and record decisions; do not scaffold, plan milestones, or implement code. Do not write SpecX project artifacts in this task.

**Definition of Done:** Every applicable node is locked to exactly one choice, all ambiguous answers are resolved, and no unsupported combination remains.

Actions:

- Classify the project into one archetype using the SpecX spec Architecture Decision Matrix rules.
- Execute the decision nodes in order.
- Ask no more than two multiple-choice questions per turn.
- Mark exactly one recommendation for each question.
- Allow the user to change a recommendation.
- Normalize and validate every answer before locking it.

Stop on an unsupported combination or unresolved ambiguous answer.
