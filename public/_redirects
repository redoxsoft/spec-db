# SPA fallback is configured in wrangler.jsonc:
#   assets.not_found_handling = "single-page-application"
#
# Do not add `/* /index.html 200` here — Cloudflare Workers rejects it as an
# infinite loop with default HTML handling (API code 100324).
